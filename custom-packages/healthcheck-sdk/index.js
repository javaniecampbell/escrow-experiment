const { HealthCheckStrategy } = require("./HealthCheckStrategy");
const { HealthCheck } = require("./HealthCheck");
const { HealthCheckContext } = require("./HealthCheckContext");

module.exports = {
    HealthCheck,
    HealthCheckStrategy,
    HealthCheckContext
};

module.exports.__default = {
    HealthCheck,
    HealthCheckStrategy,
    HealthCheckContext
};